[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["python -m venv /opt/venv && /opt/venv/bin/pip install fastapi uvicorn pydantic"]

[start]
cmd = "/opt/venv/bin/python -c \"import uvicorn; uvicorn.run('app.main:app', host='0.0.0.0', port=8000)\""